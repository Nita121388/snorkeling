# Workflow to bump the upstream package version, commit it, and create a Snorkeling release tag.
# Tag format: snorkeling-v<upstream-version>-local.<local-version>
# Example: snorkeling-v0.14.5-beta.4-local.0.0.1

name: Bump Version
run-name: "branch: ${{ github.ref_name }}; semver-bump: ${{ inputs.bump }}; prerelease: ${{ inputs.is-prerelease }}; local: ${{ inputs.local-version }}"
on:
    workflow_dispatch:
        inputs:
            bump:
                description: SemVer Bump
                required: true
                type: choice
                default: none
                options:
                    - none
                    - patch
                    - minor
                    - major
            is-prerelease:
                description: Is Prerelease
                required: true
                type: boolean
                default: true
            local-version:
                description: Local version segment appended to upstream version, e.g. 0.0.1
                required: true
                type: string
                default: "0.0.1"
            tag-extra-suffix:
                description: Optional extra tag suffix for retries, e.g. -r2
                required: false
                type: string
                default: ""
env:
    NODE_VERSION: 22
jobs:
    bump-version:
        runs-on: ubuntu-latest
        permissions:
            contents: write
        steps:
            - uses: actions/checkout@v6
              with:
                  fetch-depth: 0

            # General build dependencies
            - uses: actions/setup-node@v6
              with:
                  node-version: ${{env.NODE_VERSION}}
                  cache: npm
                  cache-dependency-path: package-lock.json
            - uses: nick-fields/retry@v4
              name: npm ci
              with:
                  command: npm ci --no-audit --no-fund
                  retry_on: error
                  max_attempts: 3
                  timeout_minutes: 5
            - name: Install Task
              uses: arduino/setup-task@v2
              with:
                  version: 3.x
                  repo-token: ${{ secrets.GITHUB_TOKEN }}

            - name: "Bump Version: ${{ inputs.bump }}"
              id: bump-version
              run: echo "WAVETERM_VERSION=$( task version -- ${{ inputs.bump }} ${{inputs.is-prerelease}} )" >> "$GITHUB_OUTPUT"
              shell: bash

            - name: "Commit version bump: ${{ steps.bump-version.outputs.WAVETERM_VERSION }}"
              id: commit-version
              run: |
                  set -euo pipefail
                  VERSION="${{ steps.bump-version.outputs.WAVETERM_VERSION }}"
                  if git diff --quiet -- package.json; then
                      echo "package.json already at version $VERSION, skipping commit."
                      echo "COMMIT_SHA=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
                      exit 0
                  fi

                  git config user.name "github-actions[bot]"
                  git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
                  git add package.json
                  git commit -m "chore: bump package version to $VERSION"
                  git push origin "HEAD:${{ github.ref_name }}"
                  echo "COMMIT_SHA=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

            - name: "Create snorkeling tag"
              run: |
                  set -euo pipefail
                  VERSION="${{ steps.bump-version.outputs.WAVETERM_VERSION }}"
                  LOCAL_VERSION="${{ inputs.local-version }}"
                  if [[ ! "$LOCAL_VERSION" =~ ^[0-9]+(\.[0-9]+){2}$ ]]; then
                      echo "Invalid local-version: $LOCAL_VERSION (expected format like 0.0.1)"
                      exit 1
                  fi

                  TAG="snorkeling-v${VERSION}-local.${LOCAL_VERSION}${{ inputs.tag-extra-suffix }}"
                  TARGET_SHA="${{ steps.commit-version.outputs.COMMIT_SHA }}"
                  if [ -z "$TARGET_SHA" ]; then
                      TARGET_SHA="$(git rev-parse HEAD)"
                  fi

                  git fetch --tags origin
                  if git rev-parse -q --verify "refs/tags/$TAG" >/dev/null; then
                      echo "Tag $TAG already exists, skipping tag push."
                      exit 0
                  fi

                  git tag "$TAG" "$TARGET_SHA"
                  git push origin "refs/tags/$TAG"
                  echo "Created tag $TAG at $TARGET_SHA"
              shell: bash
